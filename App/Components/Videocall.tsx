import React, { useState } from 'react';
import { SafeAreaView } from 'react-native';
import AgoraUIKit from 'agora-rn-uikit'; // Importing without PropsInterface

const Videocall = () => {
//   const [videocall, setVideocall] = useState(true);

//   const connectonData = {   appId: '173abcdc10fd4d67b64438d71456021e',
//   channel: 'test'};
// //  const callbacks = { EndCall: () => setVideocall(false) };

//   return videocall ? (
//     <SafeAreaView>
//       <AgoraUIKit connectionData={connectonData}  />
//     </SafeAreaView>
//   ) : null;
};

export default Videocall;
